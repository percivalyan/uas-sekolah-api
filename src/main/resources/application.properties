spring.application.name=sekolah
spring.datasource.url=jdbc:mysql://localhost:3306/uas_sekolah
spring.datasource.username=root
spring.datasource.password=
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Pengaturan CORS (opsional, tambahkan jika diperlukan)
# spring.mvc.cors.allowed-origins=http://localhost:8081
# spring.mvc.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
# spring.mvc.cors.allowed-headers=*
# spring.mvc.cors.allow-credentials=true

# Set maximum file upload size to 100MB
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=100MB
